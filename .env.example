# Redis RDI CTF Environment Configuration
# Copy this file to .env and update with YOUR specific connection details

# =============================================================================
# REDIS CLOUD CONNECTION (REQUIRED)
# =============================================================================
# Get your Redis Cloud connection details from: https://redis.com/try-free/
# Format: redis://[username]:[password]@[host]:[port]
# Example: redis://default:W9EWqRUhjTD2MbIRWHt4G7stdWg0wy2p@redis-17173.c14.us-east-1-2.ec2.redns.redis-cloud.com:17173

REDIS_URL=redis://default:YOUR_PASSWORD@YOUR_HOST:YOUR_PORT
REDIS_HOST=YOUR_HOST.redns.redis-cloud.com
REDIS_PORT=YOUR_PORT
REDIS_USERNAME=default
REDIS_PASSWORD=YOUR_PASSWORD

# =============================================================================
# LOCAL POSTGRESQL CONFIGURATION
# =============================================================================
# These will be used for your local PostgreSQL installation
POSTGRES_USER=rdi_user
POSTGRES_PASSWORD=rdi_password
POSTGRES_DB=rdi_db
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# Full PostgreSQL connection string
POSTGRES_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOST}:${POSTGRES_PORT}/${POSTGRES_DB}

# =============================================================================
# REDIS RDI CONFIGURATION
# =============================================================================
# Redis Data Integration platform settings
RDI_HOST=localhost
RDI_PORT=8080
RDI_API_URL=http://localhost:8080

# =============================================================================
# CTF SPECIFIC SETTINGS
# =============================================================================
CTF_MODE=enabled
FLAG_INJECTION_ENABLED=true
DEBUG=false
LOG_LEVEL=info

# =============================================================================
# REDISINSIGHT CONFIGURATION (LOCAL)
# =============================================================================
# RedisInsight will connect to your Redis Cloud instance using the REDIS_URL above
REDISINSIGHT_PORT=8001

# =============================================================================
# INSTRUCTIONS
# =============================================================================
# 1. Sign up for Redis Cloud: https://redis.com/try-free/
# 2. Create a new database and get your connection string
# 3. Update the REDIS_* variables above with your details
# 4. Install PostgreSQL locally (see SETUP_INSTRUCTIONS.md)
# 5. Install RedisInsight locally (see SETUP_INSTRUCTIONS.md)
