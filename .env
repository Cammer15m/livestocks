# Redis RDI CTF Environment Configuration

# PostgreSQL Configuration (pre-configured in container)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=rdi_db
DB_USER=rdi_user
DB_PASSWORD=rdi_password

# Redis Configuration
# For Redis Cloud (recommended - get free account at redis.com):
# Copy the complete connection string from Redis Cloud dashboard
REDIS_URL=redis://default:password@redis-17173.c14.us-east-1-2.ec2.redns.redis-cloud.com:17173

# For local Redis (uncomment these and comment REDIS_URL above):
# REDIS_HOST=localhost
# REDIS_PORT=6379
# REDIS_PASSWORD=

# CTF Settings
CTF_MODE=enabled
FLAG_INJECTION_ENABLED=true